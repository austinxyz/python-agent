# 2026-05-16 开发日志

## nas-https — Group 2 + Group 3 + Group 4

**OpenSpec change:** `nas-https`
**Commits:** `618c9d7` (Group 2 backend)；Group 3 + 4 uncommitted（本次 session 完成）

---

### Group 2 — 后端：`@require_admin` + tailscale_service + cert-status 端点

| # | 内容 | 状态 |
|---|------|------|
| 2.1–2.3 | RED tests：`test_admin_routes.py` — 7 个用例（admin 200、member 403、unauth 401、CLI 失败 → offline fallback；`@require_admin` 单独测试） | ✅ |
| 2.2 | GREEN：`tailscale_service.py` — `subprocess.run(["tailscale", "status", "--json"])`，`cryptography` 读证书 expiry，graceful offline fallback | ✅ |
| 2.4 | GREEN：`middleware.py` — `@require_admin` decorator，403 for member | ✅ |
| 2.5 | GREEN：`routes/admin.py` — blueprint `admin_bp`，`GET /api/admin/cert-status` | ✅ |
| 2.6 | pytest：全绿（Group 2 上线时 317 pass） | ✅ |
| 2.Z | Code review：CRITICAL/HIGH 均无，proceed | ✅ |

---

### Group 3 — 前端：AdminCertView + router + nav slot

| # | 内容 | 状态 |
|---|------|------|
| 3.2 | RED：`AdminCertView.test.js` — 6 个 token/text assertion（healthy/warning/error 三态） | ✅ |
| 3.3 | GREEN：`AdminCertView.vue` — 60s polling，pillVariant 计算，三态样式 | ✅ |
| 3.4 | VISUAL DIFF：dev stack 重 build，Notion token 类由 vitest 断言覆盖 | ✅ |
| 3.5 | RED：`router-guard.test.js` — admin 到 `/admin/cert`，member → `/chat`，logged-out → `/login?redirect=/admin/cert` | ✅ |
| 3.6 | GREEN：`router/index.js` — 新增路由，`requiresAuth+requiresAdmin` | ✅ |
| 3.7 | RED：`AppLayout.test.js` — admin 看到侧边栏链接，member 看不到 | ✅ |
| 3.8 | GREEN：`AppLayout.vue` — 底部 admin section，Shield 图标 | ✅ |
| 3.9 | RED：`MeView.test.js` — admin 看到移动端链接，member 看不到 | ✅ |
| 3.10 | GREEN：`MeView.vue` — 「管理」区块，「证书与 Tailscale 状态」链接 | ✅ |
| 3.11 | vitest：220 pass（含 HIGH fix 后新增的 2 个测试） | ✅ |
| 3.Z | Code review：2 个 HIGH issues 修复（fetchStatus 保留缓存数据 + expired cert 测试） | ✅ |

**Code review findings (Group 3):**
| Level | Issue | Fix |
|-------|-------|-----|
| HIGH | `fetchStatus` catch 分支清空 `status`，transient 失败时抹掉好数据 | 保留 `status.value`，新增 `fetchError` ref；模板改为 banner-in-rows 结构 |
| HIGH | 负 `days_remaining`（证书已过期，tailscale online）未测试 | 新增 `EXPIRED` fixture + test case |

---

### Group 4 — 部署配置

| # | 内容 | 状态 |
|---|------|------|
| 4.1 | `docker-compose.prod.yml`：`"8910:3000"` → `"127.0.0.1:8910:3000"`（loopback-only） | ✅ |
| 4.3 | `CLAUDE.md`：新增「Tailscale HTTPS」小节（Docker 路径安装、setup 命令、2 个 pitfall） | ✅ |
| 4.Z | Code review：1 个 IMPORTANT fix（api container 缺少 tailscale certs bind-mount） | ✅ |

**Code review findings (Group 4):**
| Level | Issue | Fix |
|-------|-------|-----|
| IMPORTANT | api container 无 tailscale certs bind-mount，cert-status 在 NAS 上会返回 offline fallback | `docker-compose.prod.yml` 加 `/volume1/docker/tailscale/certs:/tailscale/certs:ro`；`test_compose_prod.py` 加 regression test |

---

### Group 6 — 验证

| Check | Result |
|-------|--------|
| `pytest` | **318 passed**, 1 skipped |
| `vitest` | **220 passed** |
| `e2e integration.spec.ts` | **5 passed** |
| `console.log` audit | 干净 |
| `user_id` filter audit | 无新增违规 |
| CLAUDE.md updated | ✅ |

---

### 待完成（Group 5 + 6.6 剩余）

- [ ] Group 5：build-and-push → NAS 更新 `.env`（remove `SESSION_COOKIE_SECURE=false`，set `APP_BASE_URL=https://python-agent.tail67f33e.ts.net`，add `GOOGLE_CLIENT_ID`）→ UGOS Pull + Apply → 冒烟测试
- [ ] 4.2：NAS `.env` 手动更新（Group 5 时一并做）
- [ ] 6.6 更新：等 Group 5 完成后补填镜像 SHA + Google OAuth client ID prefix + 家人 onboarding 状态

# 2026-05-17 开发日志

## nas-https — Group 5 收尾 + 归档

**OpenSpec change:** `nas-https`（归档为 `2026-05-17-nas-https`）

---

### Google Sign-In `renderButton()` 修复过程

| # | 问题 | 修复 | Commit |
|---|------|------|--------|
| 1 | `gsiReady` + `v-if` 导致容器 div 在 `initGsi()` 执行时不在 DOM | 容器改为常驻 DOM，loading text 用 `v-if="!gsiReady"` 放在内部 | `d4dec5f` |
| 2 | `watch({ immediate: true })` + `nextTick` 组合在 config 预缓存时仍有时序问题 | 改用 `watch([gsiReady, googleBtnContainer], ...)` 组合监听，两者均就绪才调 `initGsi()` | `10e740a` |
| 3 | iOS Chrome（WebKit）不渲染 Google iframe 按钮 | 精简 `renderButton()` 参数（去掉 locale/width/shape）；iOS 仍无效 | `565c25e` |
| 决策 | iOS WebKit 对第三方 iframe 有限制，`renderButton()` 和 `prompt()` 均无效 | 暂不支持 iOS Google 登录；邮箱+密码仍可用 | — |

---

### Group 5 烟雾测试结果

| # | 内容 | 状态 |
|---|------|------|
| 5.7 | 蜂窝网络访问 `https://python-agent.tail67f33e.ts.net/` | ✅ |
| 5.8 | 家人 onboarding（Tailscale 邀请 + CLI invite 链接，端到端验证） | ✅ |
| 5.9 | `http://10.0.0.20:8910/` 无法访问（loopback-only 绑定生效） | ✅ |

---

### 归档

```
openspec archive nas-https
→ 2026-05-17-nas-https
```

Capability spec 新建：`openspec/specs/nas-https/spec.md`
Delta spec 更新：`multi-user-auth` · `frontend-scaffold`

---

### 最终镜像

`v20260517-565c25e`（含所有 GSI renderButton 修复）

---

### 待完成

无。nas-https change 完全关闭。
